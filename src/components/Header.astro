<script>
// JavaScript-funktionen er nu sikret med nul-tjek.
document.addEventListener("DOMContentLoaded", () => {
  const burgerMenu = document.querySelector("#burger-menu");
  const navItems = document.querySelector(".nav-items");

  if (burgerMenu && navItems) {
    burgerMenu.addEventListener("click", () => {
      navItems.classList.toggle("active");
    });
  } else {
    console.warn("Burger menu or navigation items are not found in the DOM.");
  }
});

</script>

<header>
    
    <div class="logo"><a href="index.astro"><img src="/public/logo/logo_sort.svg" alt="Logo"></a></div>
    <button id="burger-menu" class="burger-menu" aria-label="Toggle navigation">
        <span></span>
        <span></span>
        <span></span>
      </button>
    <ul class="nav-items">
    <li><a class="about" href="about.astro">ABOUT</a></li>
    <li><a class="services" href="services.astro">SERVICES</a></li>
    <li><a class="social" href="socialcalendar.astro">SOCIAL CALENDAR</a></li>
    </ul>
</header>

<style>
    header {
        margin: 2%;
        display: flex;
        justify-content: space-between;
        align-items: center;
        position: relative;
    }

    .logo {
        flex-shrink: 0; 
    }
img {
    width: 10rem;
}
ul.nav-items {
    display: flex;
   list-style: none;
    gap: 2.5rem;
    margin-top: 2%;  
    padding: 2%; 
}

ul.nav-items li {
    color: black;
  text-decoration: none;  
  font-family: "Roboto Mono", monospace;
  font-style: normal;
  font-size: 1.5rem;
}

ul.nav-items a {
    color: black;
    text-decoration: none; 
    display: block; 
}

ul.nav-items a:hover {
    text-decoration: underline;   
}

.burger-menu {
    display: none;
    flex-direction: column;
    gap: 0.3rem;
    background: none;
    border: none;
    cursor: pointer;
  }

  .burger-menu span {
    display: block;
    width: 2rem;
    height: 0.25rem;
    background-color: black;
    border-radius: 5px;
  }

  @media (max-width: 768px) {
    .burger-menu {
      display: flex;
    }

    ul.nav-items {
      display: none;
      flex-direction: column;
      gap: 1rem;
      position: absolute;
      top: 100%;
      right: 0;
      background-color: white;
      padding: 1rem;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      z-index: 1000;
    }

    ul.nav-items.active {
      display: flex;
    }
  }

</style>

